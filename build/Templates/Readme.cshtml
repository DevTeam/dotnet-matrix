@model Build.Targets.ReadmeModel
@{
    var categoryCount = Model.Categories.Count;
    var libraryCount = Model.Categories
        .SelectMany(category => category.Libraries)
        .Select(library => library.Name)
        .Distinct(StringComparer.OrdinalIgnoreCase)
        .Count();
    var scenarioCount = Model.Categories.Sum(category => category.Features.Count);
}
<!-- Generated by build/Templates/Readme.cshtml. Do not edit README.md directly. -->

<p align="center">
  <img src="src/Matrix.Web/wwwroot/icon.svg" width="96" height="96" alt=".NET Matrix logo">
</p>

<h1 align="center">.NET Matrix</h1>

<p align="center">
  <strong>Evidence, not faith.</strong><br>
  Choose .NET libraries with confidence using reproducible feature,
  execution-time and allocation comparisons.
</p>

<p align="center">
  <a href="@Model.ApplicationUrl"><strong>Open the interactive matrix →</strong></a>
  ·
  <a href="#how-to-read-the-results">How to read the results</a>
  ·
  <a href="#reproduce-the-results">Reproduce the results</a>
  ·
  <a href="#link-to-a-category-or-a-library">Link to the matrix</a>
</p>

<table align="center">
<tr>
<td width="180" align="center"><strong>@categoryCount</strong><br><sub>CATEGORIES</sub></td>
<td width="180" align="center"><strong>@libraryCount</strong><br><sub>LIBRARIES</sub></td>
<td width="180" align="center"><strong>@scenarioCount</strong><br><sub>SCENARIOS</sub></td>
</tr>
</table>

## Explore the matrix

| Category | Leader | Libraries | Scenarios |
|---|---|---:|---:|
@foreach (var category in Model.Categories)
{
    var categoryLeader = category.Rating.FirstOrDefault();
<text>| [**@category.Name**](#@category.Anchor) | @if (categoryLeader is null)
{<text>Not rated</text>}
else
{<text>[@categoryLeader.Name](@(Model.ApplicationUrl)?category=@category.Id&library=@categoryLeader.Id)</text>} | @category.Libraries.Count | @category.Features.Count |
</text>
}

## How to read the results

- **Correctness comes first.** Every implementation is checked against the same feature contract before its benchmark results are compared.
- **Lower is better.** Overview charts show execution time and allocated memory together.
- **Medals reward consistency.** Gold, silver and bronze are awarded to the first three places in every benchmark overview.
- **The Web application is the primary explorer.** Use it to select libraries and switch between overview, features, benchmarks and environment details.

The README is a generated snapshot of the reports committed to this repository.

@foreach (var category in Model.Categories)
{
    var leader = category.Rating.FirstOrDefault();
    var additionalOverviewCount = Math.Max(0, category.Overviews.Count - 1);
<text>
## @category.Name

@if (leader is not null)
{
<blockquote>
<strong><a href="@(Model.ApplicationUrl)?category=@category.Id&amp;library=@leader.Id">@leader.Name</a></strong> leads the current rating · @category.Libraries.Count libraries · @category.Features.Count scenarios
</blockquote>
}
else
{
<blockquote>
@category.Libraries.Count libraries · @category.Features.Count scenarios · not currently rated
</blockquote>
}

<p><a href="@(Model.ApplicationUrl)?category=@category.Id"><strong>Explore interactively →</strong></a></p>

@if (category.Rating.Count > 0)
{
<text>
### Rating

| # | Library | 🥇 | 🥈 | 🥉 | Won |
|---:|---|---:|---:|---:|---|
@foreach (var item in category.Rating)
{
<text>| @item.Place | [**@item.Name**](@(Model.ApplicationUrl)?category=@category.Id&library=@item.Id) | @(item.Gold == 0 ? "" : item.Gold.ToString()) | @(item.Silver == 0 ? "" : item.Silver.ToString()) | @(item.Bronze == 0 ? "" : item.Bronze.ToString()) | @item.Awards |
</text>
}

</text>
}
### Benchmark overview

@if (category.Overviews.Count > 0)
{
    var primaryOverview = category.Overviews[0];
<text>**@primaryOverview.Name**

![@category.Name @primaryOverview.Name benchmark overview](@primaryOverview.Path)

</text>
}
@if (additionalOverviewCount > 0)
{
<details>
<summary><strong>More overview charts (@additionalOverviewCount)</strong></summary>

@foreach (var chart in category.Overviews.Skip(1))
{
<text>#### @chart.Name

![@category.Name @chart.Name benchmark overview](@chart.Path)

</text>
}
</details>
}

<details>
<summary><strong>Compared libraries (@category.Libraries.Count)</strong></summary>

<table>
@foreach (var library in category.Libraries)
{
<tr>
<td width="64"><img src="@library.Logo" width="48" height="48" alt="@library.Name logo"></td>
<td><strong>@if (library.Url is not null)
{<a href="@library.Url">@library.Name</a>}
else
{@library.Name}</strong>@if (!string.IsNullOrWhiteSpace(library.Version))
{<text> @library.Version</text>}<br>@library.Description</td>
<td width="100" align="right"><a href="@(Model.ApplicationUrl)?category=@category.Id&amp;library=@library.Id">Compare →</a></td>
</tr>
}
</table>

</details>

<details>
<summary><strong>Benchmark scenarios (@category.Features.Count)</strong></summary>

@foreach (var feature in category.Features)
{
<text>#### @feature.Order.ToString("00") · @feature.Name

@if (feature.Description is not null)
{
<text>@feature.Description

</text>
}
![@category.Name @feature.Name benchmark](@feature.ChartPath)

</text>
}
</details>

</text>
}

## Reproduce the results

### Environment requirements

The complete workflow requires:

- a 64-bit operating system supported by the .NET 10 SDK;
- the .NET 10 SDK available as `dotnet` on `PATH`;
- access to the configured NuGet sources on the first run, or a populated local
  package cache;
- a writable repository checkout and several GB of free space for build,
  BenchmarkDotNet, report, and Web artifacts;
- at least 8 GB of RAM as a practical minimum.

For meaningful performance comparisons, run on an otherwise idle machine,
connected to power, without a debugger, with a fixed performance-oriented power
profile. Do not combine results produced by different frameworks, runtimes,
operating systems, architectures, or processors without reviewing the recorded
benchmark environment.

Run the complete reproducible workflow:

```powershell
dotnet run --project .\build -- reproduce
```

The target validates and benchmarks every library, regenerates reports, charts,
metadata, README, and Rider configurations, and starts the local Web application
on an automatically selected available port. Use `--no-browser` to suppress
automatic browser opening. Press `Ctrl+C` to stop the local application.

To use the reports already on disk and go directly to artifact generation and
the local application:

```powershell
dotnet run --project .\build -- reproduce --skip-benchmarks
```

Child-process output is saved under `artifacts/logs`; the console shows concise
progress and prints the relevant log tail if an operation fails.

To regenerate source-controlled artifacts without starting the local application:

```powershell
dotnet run --project .\build -- finalize-commit
```

> Running the complete benchmark matrix can take a long time. Use
> `dotnet run --project .\build -- --help` to find commands for a single category
> or library.

## Link to a category or a library

The application reads three optional query parameters, so a page about a library can
link straight to the relevant comparison instead of the front page:

| Link | Opens |
|---|---|
| [@(Model.ApplicationUrl)](@Model.ApplicationUrl) | the default category, newest reports |
| [@(Model.ApplicationUrl)?category=@Model.SampleCategory](@(Model.ApplicationUrl)?category=@Model.SampleCategory) | that category |
| [@(Model.ApplicationUrl)?library=@Model.SampleLibrary](@(Model.ApplicationUrl)?library=@Model.SampleLibrary) | the library overview, in its own category |
| [@(Model.ApplicationUrl)?version=latest&library=@Model.SampleLibrary](@(Model.ApplicationUrl)?version=latest&library=@Model.SampleLibrary) | the same overview, newest release named explicitly |

The three are independent and combine freely; the links in the tables above are the
same thing with the values filled in.

### `category`

Accepts the category id or its displayed name, for example `?category=@Model.SampleCategory`.
Matching ignores case, spaces and punctuation.

### `library`

Accepts the library id, its displayed name or its NuGet package id, and finds the
category on its own, so `?library=@Model.SampleLibrary` needs no `category` beside it.
Add `category` only to break a tie between two categories that use the same library
id. The library overview opens as a dialog over the matrix.

### `version`

Pins the reports to one release. It takes a release name exactly as the picker shows
it, for example `?version=1.2.0`, or the word `latest`. Unlike the other two, the
value is matched exactly: dropping punctuation would make `1.2.3` and `12.3`
indistinguishable.

Leave it out to follow every new release — a link without it always shows the newest
reports, which is what a page about a library normally wants. Use it when the link
has to keep showing the numbers it was written against, for example next to a quoted
result in an article.

The release picker in the application writes the parameter into the address, and the
copy buttons follow the same rule: the release appears in the copied link only while
an older one is being viewed.

An unknown value of any of the three falls back to the default view rather than to an
error, so a typo in a link published elsewhere still lands the reader in the matrix.
